const quarterValue = (inputNum) => {
    const outputNum = inputNum / 4
    return outputNum
}

// Invoke the function and store the return value
const result = quarterValue(600)

// Log the return value
console.log(result)